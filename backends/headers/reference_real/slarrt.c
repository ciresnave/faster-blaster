/* Threshold */
#
void slarrt_(int* n, float* d, float* e, float* rtol1, float* rtol2, int* offset, int* cnt, int* nval, int* indx, int* ndim, int* rindex, int* pindex, int* iblock, int* indexw, float* dstats, int* info) {
    if (*n <= 0) { *info = -1; return; }
    *info = 0;
}
