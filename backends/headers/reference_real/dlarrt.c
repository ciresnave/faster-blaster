/* Threshold */
#
void dlarrt_(int* n, double* d, double* e, double* rtol1, double* rtol2, int* offset, int* cnt, int* nval, int* indx, int* ndim, int* rindex, int* pindex, int* iblock, int* indexw, double* dstats, int* info) {
    if (*n <= 0) { *info = -1; return; }
    *info = 0;
}
