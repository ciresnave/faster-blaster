/* slars - Bidiagonal auxiliary vector */
#
void slars_(char* side, int* pivot, int* direct, int* m, int* n, int* k, float* a, int* lda, int* ipiv, float* work) {
    if (*m <= 0 || *n <= 0) return;
}
